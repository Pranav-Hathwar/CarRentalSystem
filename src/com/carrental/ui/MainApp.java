package com.carrental.ui;

import com.carrental.interfaces.PaymentProcessor;
import com.carrental.model.Booking;
import com.carrental.model.Car;
import com.carrental.service.BookingService;
import com.carrental.service.VehicleService;
import com.carrental.threads.BookingExpiryThread;

import java.util.List;
import java.util.Scanner;

public class MainApp {
    /*
     * private static VehicleService vehicleService = new VehicleService();
     * private static BookingService bookingService = new BookingService();
     * private static Scanner scanner = new Scanner(System.in);
     * 
     * public static void main(String[] args) {
     * // ... (legacy code commented out to support web-only mode)
     * System.out.println("Legacy CLI mode disabled. Please run WebServerLauncher."
     * );
     * }
     */
    public static void main(String[] args) {
        System.out.println("Legacy CLI mode disabled. Please run WebServerLauncher.");
    }
}
